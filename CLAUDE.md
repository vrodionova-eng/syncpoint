# [ШАБЛОН] Новый проект — Битрикс24 / VibeCode

---

## КЛОДУ: Прочитай это первым делом

Ты помогаешь ученику курса VibeCoding запустить новый проект.
Ученик — не разработчик. Объясняй просто, без технического жаргона.
Если встречается незнакомый термин — объясни его в скобках.

**Проверь наличие файла `.onboarding-done` в корне проекта:**
- Файла нет → онбординг не пройден. Напиши: *"Привет! Давай настроим проект. Я задам несколько вопросов — по одному. Начнём?"* Затем следуй протоколу онбординга.
- Файл есть → онбординг пройден. Читай раздел "О ПРОЕКТЕ" и работай в рамках проекта.

**Два режима работы:**
- **Чат** (сотрудник пишет тебе вопросы) — отвечай, объясняй, запускай команды сам через агента.
- **Задача из Kanban** (тебе прилетает задача без диалога) — читай CLAUDE.md, выполняй задачу автономно, без лишних вопросов. Если чего-то не хватает для выполнения — сообщи об этом в конце, не останавливайся на середине.

---

## ПРОТОКОЛ ОНБОРДИНГА

Задавай вопросы **строго по одному**. После каждого ответа — следующий вопрос.
Для каждого вопроса: объясни варианты, подсвети рекомендуемый словом **→ Рекомендую**.
После всех ответов — выполни шаги из раздела "После всех ответов".

---

### Вопрос 1 — Название и суть

*"Как называется проект и что он делает? Одной-двумя фразами."*

*(Пример: "Пульт24 — дашборд аналитики продаж для менеджеров")*

---

### Вопрос 2 — Портал Битрикс24

*"На каком портале Битрикс24 работает клиент?"*

*(Пример: company.bitrix24.ru)*

Если клиент ещё не определился — запиши как `не определён`, двигаемся дальше.

---

### Вопрос 3 — Где будет жить приложение

*"Где мы разместим приложение?"*

**Варианты:**

- **Vibecode** — хостинг от Битрикс24. Деплой одной командой, всё уже настроено.
  **→ Рекомендую для большинства проектов.**
- **Свой сервер** — полный контроль, но требует ручной настройки.
  Выбирай если: нужны особые требования к безопасности или уже есть инфраструктура.
- **Ещё не знаю** — запишем, разберёмся позже.

---

### Вопрос 4 — Как подключаемся к Битрикс24

*"Как приложение будет получать данные из Битрикс24?"*

**Варианты:**

- **API-ключ (vibe_api_...)** — один ключ на портал, все видят одни данные.
  Просто: создал ключ, вставил в настройки, работает.
  **→ Рекомендую для дашбордов и инструментов администратора.**
- **OAuth-приложение (vibe_app_...)** — каждый сотрудник входит через свой аккаунт Б24.
  Каждый видит только свои данные. Сложнее в настройке.
  Выбирай если: важно разграничение прав между сотрудниками.
- **Без Битрикс24** — приложение не работает с данными Б24 напрямую.

---

### Вопрос 5 — Где открывается приложение

*"Где пользователь будет открывать приложение?"*

**Варианты:**

- **Внутри Битрикс24** — вкладка в меню, виджет в карточке CRM, кнопка в сделке.
  Выглядит как часть Б24. **→ Рекомендую для инструментов сотрудников.**
- **Отдельная страница** — открывается в браузере, не зависит от Б24.
  Выбирай если: это отдельный сервис или клиент не использует Б24 интерфейс.
- **Оба варианта** — и внутри Б24, и как отдельная страница.

---

### Вопрос 6 — Нужен ли серверная часть (бэкенд)

*"Приложению нужна серверная часть?"*

**Варианты:**

- **Нет, только фронт** (HTML + JS) — подходит для простых дашбордов и виджетов.
  Данные берутся прямо из Битрикс24. **→ Рекомендую если нет сложной логики.**
- **Да, Node.js** — нужен если: авторизация пользователей, хранение данных,
  сложная обработка, интеграция с другими сервисами.
- **Не знаю** — опиши задачу, помогу определиться.

---

### Вопрос 7 — Дизайн

*"Как должно выглядеть приложение?"*

**Варианты:**

- **Стиль Битрикс24** — выглядит как часть платформы, знакомо сотрудникам.
  **→ Рекомендую для виджетов и инструментов внутри Б24.**
- **Нейтральный современный** — чистый, профессиональный вид. Подходит
  для отдельных приложений и сервисов.
- **Корпоративный стиль клиента** — используем цвета и шрифты клиента.
  Потребуется описание стиля или брендбук.

---

### Вопрос 8 — Авторизация пользователей

*"Нужно ли пользователям входить в приложение?"*

**Варианты:**

- **Не нужна** — приложение открыто для всех у кого есть ссылка.
  **→ Рекомендую если доступ уже ограничен через Битрикс24** (приложение видят только нужные сотрудники).
- **PIN-код** — простая защита: сотрудник вводит цифровой код.
  Выбирай если: нет Б24 и нужна минимальная защита (как в проекте "Балтийская группа").
- **Через Битрикс24** — сотрудник входит своим аккаунтом Б24 (OAuth).
  Выбирай если: нужно знать кто именно работает с приложением.
- **Своя авторизация** — логин/пароль или другой способ. Опиши задачу, разберёмся.

---

### После всех ответов

1. Обнови раздел "О ПРОЕКТЕ" ниже — замени все `{{PLACEHOLDER}}` реальными значениями
2. Создай файл `.onboarding-done` в корне проекта (пустой файл — маркер завершения онбординга)
3. Создай `docs/project-brief.md` с итогами онбординга
4. Запусти `npm install` и `npm run check` — убедись что API подключён
5. Скажи сотруднику: что настроено, что нужно от него дальше (например, вставить API-ключ в `.env`)

---

## О ПРОЕКТЕ

> *Этот раздел заполняется автоматически после онбординга. До онбординга — не трогать.*

**Название:** SyncPoint24
**Описание:** Система бронирования ресурсов внутри Битрикс24. Бронируется рабочая точка на время; к ней привязаны пространство и экземпляры инструментов. У записи — сотрудники-участники, услуга(и), цена, клиент из CRM.
**Портал Б24:** не определён
**Размещение:** Vibecode
**Подключение к Б24:** API-ключ портала (`VIBE_API_KEY`)
**Где открывается:** Внутри Битрикс24, пункт левого меню (`LEFT_MENU`)
**Бэкенд:** Node.js + Express, своё хранилище (JSON на диске)
**Дизайн:** Стиль Битрикс24
**Авторизация:** Отдельной нет, доступ через Б24 (права пока не разграничиваем)

**Этапы:**
- Этап 1 — справочники и каркас. Спек: `docs/superpowers/specs/2026-06-04-syncpoint24-stage1-design.md`
- Этап 2 — календарь, записи, чекбоксы участников, клиенты CRM, расчёт ЗП

**Сущности:** Пространство · Рабочая точка · Инструмент + Экземпляр · Услуга · Сотрудник · (Этап 2) Запись

**Специфика проекта** (заполняется по ходу работы):
- Ключ API: хранится в `.env` как `VIBE_API_KEY`
- Хранилище: JSON-файлы, каталог через `DATA_DIR` (локально `./data`, прод `/opt/data`)
- SERVER_ID: заполняется после первого деплоя
- App URL: заполняется после первого деплоя
- Расчёт ЗП: схема в карточке сотрудника (общий % / по услугам) — задел в Этапе 1, UI и расчёт — Этап 2

---

## СТЕК И ИНСТРУМЕНТЫ

Это стандартный стек для всех проектов. Не менять без необходимости.

- **Node.js 20** + ES modules (`"type": "module"` в package.json)
- **Express 4** — веб-сервер
- **Vanilla JS + CSS** — фронтенд без фреймворков (меньше зависимостей → быстрый деплой)
- **VibeCode API** — прокси к данным Битрикс24: `https://vibecode.bitrix24.tech/v1`
- **Chart.js** (через CDN) — если нужны графики

---

## СТРУКТУРА ФАЙЛОВ

```
[название-проекта]/
├── CLAUDE.md              ← этот файл (инструкции для Клода)
├── .claude/
│   └── commands/          ← скиллы проекта (/deploy, /update-brief и др.)
├── .env                   ← ключи и секреты (не попадает в git!)
├── .env.example           ← шаблон переменных (без реальных значений)
├── .gitignore
├── package.json
├── deploy.sh              ← скрипт деплоя на Vibecode
├── docs/
│   ├── project-brief.md   ← описание проекта (заполняется при онбординге)
│   └── changelog.md       ← история изменений
├── src/
│   ├── server.js          ← точка входа Express
│   ├── check.js           ← проверка подключения к API
│   ├── utils/
│   │   └── api.js         ← обёртка над VibeCode API
│   ├── routes/            ← API-маршруты
│   ├── services/          ← бизнес-логика
│   └── public/            ← фронтенд
│       ├── index.html
│       ├── css/
│       │   ├── theme-b24.css      ← стиль Битрикс24
│       │   └── theme-neutral.css  ← нейтральный стиль
│       └── js/
│           ├── app.js     ← точка входа фронтенда
│           └── bitrix.js  ← обёртка над Б24 JS SDK
└── data/                  ← кеш и локальные данные (не попадает в git!)
```

---

## КЛЮЧИ И БЕЗОПАСНОСТЬ

**Правило №1: Ключи никогда не попадают в код и в git.**

Все ключи хранятся в файле `.env`. Этот файл есть в `.gitignore`.
В git попадает только `.env.example` — шаблон без реальных значений.

**Типы ключей VibeCode:**

| Тип | Префикс | Когда использовать |
|-----|---------|-------------------|
| API-ключ | `vibe_api_...` | Личный дашборд, скрипты, один портал |
| OAuth-приложение | `vibe_app_...` | Команда, у каждого свои права в Б24 |
| Менеджмент | `vibe_live_...` | Только для администрирования, не для данных |

**Переменные `.env` (стандартный набор):**

```
# VibeCode
VIBE_API_KEY=vibe_api_...         # API-ключ портала
VIBE_BASE_URL=https://vibecode.bitrix24.tech/v1

# Сервер (заполняется после первого деплоя)
SERVER_ID=                         # ID сервера на Vibecode
PORT=3000
NODE_ENV=production
```

**Где получить ключ:** https://vibecode.bitrix24.tech → Ключи API → Создать

**Правило №2: При подозрении на утечку ключа — сразу отозвать** в личном кабинете Vibecode и создать новый. Обновить `.env` на сервере.

---

## MCP-СЕРВЕРЫ

MCP — это инструменты, которые позволяют Клоду напрямую обращаться к API и документации прямо из редактора. Подключаются через `.claude/settings.local.json`.

**Два обязательных MCP для всех проектов:**

1. **vibecode** (`mcp-vibe-api`) — прямой доступ к VibeCode API: сущности, боты, чаты, телефония
2. **bitrix24-docs** (`mcp-bitrix24-docs`) — поиск по официальной документации Битрикс24 REST API

**Установка (один раз на машину):**
```bash
npm install -g mcp-vibe-api mcp-bitrix24-docs
```

**Файл `.claude/settings.local.json`** (уже создан в шаблоне):
```json
{
  "permissions": {
    "allow": [
      "mcp__vibecode",
      "mcp__bitrix24_docs",
      "Bash(npm run:*)",
      "Bash(npm install:*)",
      "Bash(node:*)",
      "Bash(curl:*)",
      "Bash(bash:*)",
      "Bash(tar czf:*)",
      "Bash(chmod +x:*)",
      "Bash(git init:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "WebSearch",
      "WebFetch(domain:vibecode.bitrix24.tech)",
      "WebFetch(domain:apidocs.bitrix24.ru)",
      "WebFetch(domain:dev.1c-bitrix.ru)"
    ]
  },
  "mcpServers": {
    "vibecode": {
      "command": "mcp-vibe-api",
      "args": ["--key", "ENV:VIBE_API_KEY"]
    },
    "bitrix24-docs": {
      "command": "mcp-bitrix24-docs",
      "args": []
    }
  }
}
```

---

## КОМАНДЫ

```bash
npm install        # установить зависимости (первый раз)
npm run check      # проверить подключение к VibeCode API → должно быть "API connection OK"
npm run dev        # запустить локально с авто-перезагрузкой
npm start          # запустить в продакшн-режиме
npm run deploy     # задеплоить на Vibecode (= bash deploy.sh)
```

**Первый запуск нового проекта:**
```bash
npm install
# вставь VIBE_API_KEY в .env
npm run check
```

---

## ПРАВИЛО ДЕПЛОЯ — ОБЯЗАТЕЛЬНО

**После КАЖДОГО изменения кода — сразу деплой:**

```bash
SERVER_ID=<id> npm run deploy
```

Локальная версия и продакшн на Vibecode **всегда должны быть идентичны**.
Не копи изменения, не откладывай деплой "на потом".

Пользователь работает с продакшном внутри Битрикс24.
Если поправил баг локально, а не задеплоил — в Б24 всё ещё старый код.

**Исключения** (деплой НЕ нужен):
- правки в `.env`, `.gitignore`, `CLAUDE.md`, `docs/` — не влияют на работу
- эксперименты в отдельной ветке, которая точно не пойдёт в прод

---

## ПАТТЕРНЫ РАЗРАБОТКИ

Эти правила выработаны из реальных проектов. Следовать им по умолчанию.

### API-клиент
- VibeCode может возвращать данные в двух форматах: `data.result` и `data.data` — всегда проверяй оба
- Пагинация: запрашивай по 50 записей, повторяй пока результат < limit
- Батчи: до 25 параллельных запросов (больше — риск попасть в rate limit: 60 запросов/мин)

### Кеширование
- Тяжёлые отчёты: храни в памяти (Map) + дублируй на диск (JSON)
- На сервере Vibecode: кеш в `/opt/data/` — переживает деплой
- Локально: папка `data/` (в .gitignore)

### Деплой на Vibecode
- `cleanDeploy=true` — чистая установка каждый раз (папка `/opt/app/` стирается)
- Данные храни в `/opt/data/`, не в `/opt/app/`
- Runtime: `node20`

### Фронтенд
- Vanilla JS + CSS — никаких фреймворков без явной необходимости
- CSS custom properties (`--var`) для цветов и отступов — легко менять тему
- Chart.js через CDN — не тащить в сборку

### Битрикс24 iframe
- Б24 загружает приложение и отправляет POST на `/` с данными о размещении
- Сервер должен отвечать на POST `/` так же как на GET `/` — отдавать `index.html`

---

## ДОКУМЕНТАЦИЯ — ПРОВЕРЯЙ КОГДА ЧТО-ТО НЕ РАБОТАЕТ

Битрикс24 и VibeCode регулярно обновляют API и документацию.
Если что-то сломалось или не идёт — сначала проверь актуальную документацию.

**Главная точка входа — живая документация под твой ключ:**
```bash
curl -H "X-Api-Key: $VIBE_API_KEY" https://vibecode.bitrix24.tech/v1/me
```
Возвращает список всех доступных эндпоинтов для этого конкретного ключа. Актуально всегда.

**Ссылки для проверки:**

| Что | Ссылка |
|-----|--------|
| VibeCode — быстрый старт | https://vibecode.bitrix24.tech/docs/quickstart |
| VibeCode — полный справочник | https://vibecode.bitrix24.tech/llms-full.txt |
| VibeCode — ключи и авторизация | https://vibecode.bitrix24.tech/docs-content/keys-auth.md |
| VibeCode — OpenAPI схема | GET /v1/openapi.json |
| Битрикс24 — OAuth и токены | https://apidocs.bitrix24.ru/settings/oauth/auto-renewal.html |
| Битрикс24 — виджеты CRM | https://apidocs.bitrix24.ru/api-reference/widgets/crm/index.html |
| Битрикс24 — справочник REST API | https://apidocs.bitrix24.ru/api-reference/ |

**Используй MCP `bitrix24-docs`** для поиска по документации прямо из редактора — быстрее чем браузер.
